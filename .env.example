# mxfilter configuration by environment variable.
#
# Copy to .env and source it, or export these however you normally do.
# Everything here can also live in $XDG_CONFIG_HOME/mxfilter/config.toml,
# and any of it can be overridden by the matching CLI flag.

# The MXRoute server your account lives on. It is per-account -- the same
# as your primary MX record -- and is shown on the panel's Email Clients
# page. It is a machine name, not your domain, and there is no sensible
# default, so this must be set.
MXROUTE_HOST=mail.example-server.mxrouteXX.com

# MXRoute usernames are the FULL email address. A bare local part will fail
# authentication on both ManageSieve and IMAP.
MXROUTE_USER=you@yourdomain.com

# --- Password: pick exactly one of the three approaches below. ------------
#
# 1. A command that prints the password on stdout. Preferred: the value
#    never sits in the environment, in your shell history, or in a file.
#    mxfilter runs it without a shell and never echoes its output.
MXROUTE_PASSWORD_CMD=pass show email/you@yourdomain.com

# 2. The password directly. Convenient, but readable by anything that can
#    read this process's environment. MXROUTE_PASSWORD wins over
#    MXROUTE_PASSWORD_CMD if both are set.
#MXROUTE_PASSWORD=

# 3. Neither -- mxfilter prompts on the terminal (getpass, no echo).

# --- Endpoints (defaults are correct for MXRoute) -------------------------

# IMAP over implicit TLS. Port 143 is also accepted and switches to STARTTLS.
MXROUTE_IMAP_HOST=mail.example-server.mxrouteXX.com
MXROUTE_IMAP_PORT=993

# ManageSieve. MXRoute documents NEITHER the port nor the TLS mode. 4190 is
# the IANA-registered port (RFC 5804) and the Dovecot default, and STARTTLS
# is Dovecot's usual mode there -- a good default, not a verified MXRoute
# fact. If connecting fails, try MXROUTE_SIEVE_TLS=ssl (implicit TLS), then
# ask MXRoute support.
MXROUTE_SIEVE_PORT=4190
MXROUTE_SIEVE_TLS=starttls

# Where the pre-upload backup of the active script is written.
# Default: $XDG_STATE_HOME/mxfilter/backups
#MXROUTE_BACKUP_DIR=
